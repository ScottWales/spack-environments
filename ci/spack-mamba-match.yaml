spack:
  # Spack packages that need to match mamba versions
  packages:
      xz:
          require: '@=5.2.6'
      libevent:
          require: '@=2.1.10'
      gsl:
          require: '@=2.7 +external-cblas'
      libxml2:
          require: '@=2.10.3'
