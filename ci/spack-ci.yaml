spack:
  view: false

  concretizer:
    unify: false

  mirrors:
    ngm-gitlab: /scratch/mirror

  gitlab-ci:
    mappings:
      - match:
          - os=amzn2023
        runner-attributes:
          tags:
            - docker
          image: spack/ubuntu-bionic
